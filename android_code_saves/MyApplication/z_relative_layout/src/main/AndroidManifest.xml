<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.z_relative_layout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eq"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">


        <activity android:name=".identity"
            android:icon="@mipmap/identity"
            android:label="@string/act_identity">

        </activity>

        <activity
            android:name=".house"
            android:icon="@mipmap/house"
            android:label="@string/act_house">

        </activity>
        <activity
            android:name=".tool"
            android:icon="@mipmap/tool"
            android:label="@string/act_tool">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>